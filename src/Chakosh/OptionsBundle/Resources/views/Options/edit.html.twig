{% extends 'ChakoshHomeBundle:Admin:base.html.twig' %}

{% block content %}
    <h1>Options edit</h1>

    {{ form_start(edit_form,{attr: {'class':'form-horizontal'}}) }}
    <div class="form-group">
      <label for="inputopttionname" class="col-sm-2 control-label">{{ form_label(edit_form.optionName) }}</label>
      <div class="col-sm-10">
        {{ form_widget(edit_form.optionName,{attr:{'class':'form-control'}}) }}
        {{ form_errors(edit_form.optionName) }}
      </div>
    </div>
    <div class="form-group">
      <label for="inputopttionvalue" class="col-sm-2 control-label">{{ form_label(edit_form.optionValue) }}</label>
      <div class="col-sm-10">
        {{ form_widget(edit_form.optionValue,{attr:{'class':'form-control'}}) }}
        {{ form_errors(edit_form.optionValue) }}
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        {{ form_widget(edit_form.submit,{attr:{'class':'btn btn-primary'}}) }}
      </div>
    </div>
          {{ form_errors(edit_form) }}
          {{ form_rest(edit_form) }}
    </form>
    {{ form_end(edit_form) }}
        <a class="btn btn-default" href="{{ path('admin_setting_') }}">
            <i class="glyphicon glyphicon-arrow-left"></i> Back to the list
        </a>
{% endblock %}